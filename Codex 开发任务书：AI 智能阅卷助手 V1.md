# **Codex 开发任务书：AI 智能阅卷助手 V1**

## **0. 项目定位**

开发一款 **Windows 优先的 AI 自动阅卷桌面应用**。项目名：gradepilot

主要使用场景：

- 中国初中物理填空题、实验填空题。
- 用户在智学网网页端批改试卷。
- 软件读取固定屏幕区域中的学生手写答案。
- 调用视觉大模型直接理解手写中文，而不是依赖传统 OCR。
- 根据教师事先配置的：
  - 题目
  - 参考答案
  - 每空评分标准
  - 特殊规则
- 自动计算总分。
- 自动点击智学网页面中的分数输入框。
- 自动输入分数。
- 自动点击“提交分数”。
- 智学网自动进入下一份后继续处理。
- 教师可以先进行试改，确认准确率后放任软件自动工作。

正式目标平台：

Windows 10 / Windows 11 x64

开发可以在 macOS 上进行，但 Windows 是唯一必须保证稳定运行的平台。

------

# **1. V1 核心原则**

V1 不追求复杂的网页 DOM 自动化，不追求浏览器扩展，不追求适应任意窗口。

用户接受：

- 浏览器窗口固定。
- 分辨率固定。
- Windows 显示缩放比例固定。
- 阅卷期间不移动智学网页面。
- 每次换环境可以重新做一次坐标标定。

因此 V1 优先选择稳定、简单、容易维护的桌面自动化方案。

### **最重要的安全原则**

AI 的判断允许存在概率性。

但：

**向智学网提交分数必须经过严格状态检查。**

任何异常、不确定、API错误、模型输出格式错误、页面异常，都不得猜测后继续提交。

------

# **2. 推荐技术栈**

建议优先：

```text
Python 3.12

GUI
PySide6

截图
mss
Pillow
OpenCV

鼠标键盘自动化
pyautogui
必要时 pywinauto

HTTP
httpx

数据
SQLite
SQLAlchemy 可选

配置
JSON / SQLite

快捷键
keyboard 或 pynput

日志
Python logging + SQLite

打包
PyInstaller

测试
pytest
```

V1 不需要：

- Electron
- 浏览器插件
- Selenium
- Playwright
- OCR 引擎
- 本地大模型
- 数据库服务器
- Docker

代码架构中应预留以后接入 DOM 自动化的可能，但 V1 不实现。

------

# **3. 项目建议命名**

暂定内部项目名：

```text
AIGrader
```

中文：

AI 阅卷助手

不要把“智学网”写入项目核心类名，以便以后兼容其他平台。

------

# **4. 建议目录结构**

```text
AIGrader/
├─ app/
│  ├─ main.py
│  │
│  ├─ ui/
│  │  ├─ main_window.py
│  │  ├─ task_editor.py
│  │  ├─ calibration_dialog.py
│  │  ├─ grading_panel.py
│  │  ├─ settings_dialog.py
│  │  └─ review_dialog.py
│  │
│  ├─ core/
│  │  ├─ grading_engine.py
│  │  ├─ task_manager.py
│  │  ├─ state_machine.py
│  │  ├─ screenshot.py
│  │  ├─ automation.py
│  │  ├─ safety.py
│  │  └─ validator.py
│  │
│  ├─ ai/
│  │  ├─ base.py
│  │  ├─ deepseek.py
│  │  ├─ schemas.py
│  │  └─ prompts.py
│  │
│  ├─ platforms/
│  │  ├─ base.py
│  │  └─ zhixue.py
│  │
│  ├─ data/
│  │  ├─ database.py
│  │  ├─ models.py
│  │  └─ repository.py
│  │
│  └─ utils/
│
├─ tests/
├─ assets/
├─ build/
├─ .github/workflows/
├─ requirements.txt
├─ README.md
└─ LICENSE
```

------

# **5. AI Provider 抽象**

不要把 DeepSeek API 写死在业务代码中。

定义：

```python
class AIProvider:
    async def grade(self, request: GradeRequest) -> GradeResult:
        ...
```

V1 实现：

```text
DeepSeekProvider
```

以后允许：

```text
QwenProvider
```

但 **V1 不要求真正完成 Qwen API**。

GUI 中可以显示：

```text
AI模型
DeepSeek
```

架构必须允许以后增加下拉模型。

------

# **6. DeepSeek API 配置**

用户已经拥有 DeepSeek API Key。

设置页：

```text
AI服务：DeepSeek

API Key：
**********************

[测试连接]

默认模型：
xxxx
```

API Key：

- 不允许提交 Git。
- 不允许写入普通明文项目 JSON。
- Windows 正式版优先使用 Windows Credential Manager 或等效安全存储。
- 开发阶段可以提供 `.env` fallback，但 `.env` 必须加入 `.gitignore`。

------

# **7. 阅卷任务模型**

每次阅卷建立一个 Task。

例如：

```text
考试：
初三年级暑假作业巩固练习（物理）

题号：
16

满分：
7

分数步长：
1

AI Provider：
DeepSeek

提交前等待：
1.0 秒

最大连续自动阅卷：
100
```

保存后下次可以继续使用。

------

# **8. 题目输入**

题目支持两种方式：

### **方式一：上传题目截图**

例如：

```text
question.png
```

### **方式二：输入题目文字**

提供：

```text
题目文字：
[_________________________________]
```

两者可以同时存在。

如果同时存在：

图片 + 文字都发送给 AI。

题目内容作为固定上下文保存。

------

# **9. 评分标准编辑器**

该项目主要处理：

一空一分的填空题。

每一个空建立独立评分项。

例如：

## **第（3）空**

```text
满分：1

参考答案：
钩码的数量

得分标准：
表达出改变钩码数量即可。

接受的表达：
钩码数量
钩码个数
增加钩码
多挂几个钩码

不接受：
钩码质量
砝码数量

特殊规则：
☐ 必须有正确单位
☐ 必须完全匹配
☐ 不允许错别字

其他说明：
____________________
```

另外整题提供：

```text
全题补充评分规则
```

允许教师用自然语言填写。

------

# **10. 默认评分原则**

AI Prompt 应明确遵守以下规则。

### **错别字**

默认：

不影响物理含义时可以给分。

但单空可以配置：

不允许错别字。

------

### **单位**

每空独立配置是否要求单位。

例如：

```text
要求单位：是
```

则：

```text
2.5 N
```

正确，而：

```text
2.5
```

不得分。

------

### **同义表达**

AI 可以结合语义判断。

教师配置：

```text
接受表达
不接受表达
```

优先级高于 AI 自己判断。

------

### **涂改**

如果可以明确判断最终保留答案：

按最终答案判分。

如果无法明确：

```json
"need_review": true
```

------

### **同一个空写两个相互矛盾的答案**

用户要求：

**直接不给分。**

不需要暂停人工复核。

例如：

```text
增大（减小）
```

该空：

```text
0分
```

判分理由注明：

同时保留互相矛盾的答案。

------

### **关键词正确但整句话物理意义错误**

不得因为出现关键词而给分。

必须判断完整语义。

------

### **字迹部分不清楚**

如果根据上下文能够高度可靠判断：

可以正常评分。

如果存在两种可能解释，而且会影响得分：

```text
need_review = true
```

------

### **数值答案**

用户要求：

默认必须符合参考答案要求的形式。

不要自动把不同形式都认为等价。

例如参考答案：

```text
0.5 A
```

不要自动因为：

```text
500 mA
```

就认定一定得分。

如允许等价表达，由教师在评分规则中明确写出。

------

# **11. 学生答案截图**

V1 不逐空框选。

只标定：

一个完整的“学生答案区域”。

即：

```text
左上角
右下角
```

截图后将整块答案区域发送给视觉模型。

AI 自己根据：

- 题号
- 小问编号
- 横线
- 空格
- 书写位置

确定各答案对应关系。

------

# **12. 坐标标定**

建立专门的 Calibration Wizard。

流程：

### **Step 1**

提示：

请点击学生答案区域左上角。

### **Step 2**

请点击学生答案区域右下角。

### **Step 3**

请点击智学网分数输入框。

### **Step 4**

请点击“提交分数”按钮。

最终保存：

```json
{
  "answer_region": {
    "x": 58,
    "y": 166,
    "width": 1316,
    "height": 500
  },
  "score_input": {
    "x": 1510,
    "y": 424
  },
  "submit_button": {
    "x": 1554,
    "y": 475
  }
}
```

提供：

重新标定

------

# **13. Dry Run：仅测试 AI**

必须实现。

按钮：

**识别当前卷**

执行：

```text
截图
↓
发送AI
↓
显示结果
```

绝对不得：

- 点击分数框
- 输入数字
- 点击提交

用于正式阅卷前调 Prompt 和评分标准。

------

# **14. AI 返回的数据结构**

禁止依靠自由文本解析分数。

必须要求结构化 JSON。

例如：

```json
{
  "recognized_answers": [
    {
      "part": "1",
      "text": "定"
    },
    {
      "part": "2",
      "text": "A"
    },
    {
      "part": "3",
      "text": "钩码的数量"
    }
  ],
  "grading": [
    {
      "part": "1",
      "score": 1,
      "max_score": 1,
      "reason": "与参考答案一致"
    },
    {
      "part": "2",
      "score": 1,
      "max_score": 1,
      "reason": "答案A正确"
    }
  ],
  "total_score": 6,
  "max_score": 7,
  "need_review": false,
  "review_reason": null,
  "summary": "第4空未满足评分标准，因此扣1分"
}
```

------

# **15. 不展示模型隐藏思维链**

GUI 中不要设计：

“AI 内部思考过程”。

只展示可核验的：

```text
识别答案
逐空得分
扣分原因
复核原因
最终分数
```

即“判分依据”。

------

# **16. AI 输出硬验证**

任何结果提交前必须经过程序验证。

例如：

```python
0 <= score <= max_score
```

并验证分数步长。

例如：

```text
满分 = 7
步长 = 1
```

只允许：

```text
0
1
2
3
4
5
6
7
```

如果 DeepSeek 返回：

```text
6.5
```

立即暂停。

不得自动四舍五入。

------

# **17. 自动阅卷状态机**

不要写成一堆 `sleep()`。

建立明确状态机。

建议：

```text
IDLE
↓
CAPTURE
↓
AI_REQUEST
↓
AI_VALIDATION
↓
SHOW_RESULT
↓
OBSERVATION_DELAY
↓
ENTER_SCORE
↓
SUBMIT
↓
WAIT_NEXT
↓
CAPTURE
```

异常状态：

```text
NEED_REVIEW
API_ERROR
PAGE_ERROR
VALIDATION_ERROR
PAUSED
STOPPED
```

------

# **18. 单份答卷完整流程**

```text
① 截取学生答案区域

② 调用 DeepSeek

③ 获取结构化结果

④ 验证 JSON

⑤ 验证分数

⑥ need_review？

YES
→ 停止任何网页操作
→ 播放提示
→ 弹出人工复核

NO
→ 显示AI结果

⑦ 等待可配置观察时间
默认 1 秒

⑧ 点击智学网分数框

⑨ Ctrl+A

⑩ 输入得分

⑪ 再检查准备提交状态

⑫ 点击“提交分数”

⑬ 等待下一份加载

⑭ 继续
```

------

# **19. 下一份试卷检测**

不要求用“截图相同”阻止重复批阅。

原因：

智学网可能因为复核机制向不同阅卷者展示完全相同的学生答案，这是正常情况。

因此：

**不得仅仅因为新截图和上一张相同，就拒绝阅卷。**

下一份检测主要用于确认：

提交后页面发生了有效刷新。

可以综合：

- 页面局部视觉变化
- 加载等待时间
- 提交按钮状态
- 固定区域变化

但不能假设：

相同答案 = 重复试卷。

------

# **20. API 错误重试**

采用：

```text
第一次失败
↓
等待约2秒

第二次失败
↓
等待约5秒

第三次失败
↓
暂停任务
```

即最多三次请求。

错误不得导致自动填分。

------

# **21. need_review 行为**

只要：

```json
"need_review": true
```

立即：

```text
暂停自动阅卷
不输入分数
不点击提交
```

人工完成处理后：

继续阅卷

无人值守状态下同样如此。

不得跳过该卷继续下一张。

------

# **22. 快捷键**

至少提供：

```text
F8
安全暂停

F9
继续

Ctrl + Alt + Q
紧急停止
```

另开启 PyAutoGUI failsafe：

鼠标快速移动到屏幕左上角 → 立即停止自动操作。

快捷键在设置中可以修改，但默认如此。

------

# **23. 自动提交观察时间**

默认：

```text
1.0 秒
```

配置范围：

```text
0～5秒
```

流程：

```text
AI判完
↓
GUI显示
↓
1秒倒计时
↓
填分并提交
```

期间按 F8：

本份不得提交。

------

# **24. 试改模式**

实现一个独立：

**试改模式**

默认：

```text
100份
```

注意：

试改模式仍然真实向智学网提交分数。

不是模拟。

用户坐旁边监控。

------

# **25. 试改统计**

统计至少包括：

```text
已处理：
67 / 100

AI直接正常：
62

AI主动要求人工复核：
3

人工复核后AI判断其实正确：
2

AI真实错判：
2
```

重点指标：

### **漏报错判**

即：

AI 没有请求人工复核，但实际上分数给错。

这是最危险的指标。

显示：

```text
漏报错判率
2 / 67 = 2.99%
```

------

# **26. 自动模式启用**

试改完成后：

```text
总数 100
真实错判 3
错判率 3%
```

如果满足用户设定阈值：

例如：

```text
≤ 5%
```

显示：

已达到自动模式参考条件。

但是：

**绝对不要自动切换自动模式。**

必须由用户自己点击：

启用自动模式

------

# **27. 连续自动阅卷上限**

默认：

```text
100份
```

可以修改。

可以关闭限制。

例如：

```text
每连续处理100份
→ 自动暂停
→ 用户点击继续
```

项目稳定以后用户可以设置：

```text
无限制
```

------

# **28. AI 判错记录**

发现错判后提供：

标记 AI 错判

保存：

```text
答案截图
题目
评分标准
DeepSeek模型
AI识别答案
AI给分
AI判分依据
正确分数
时间
```

并允许选择：

```text
□ 字迹识别错误
□ 评分标准理解错误
□ 物理语义判断错误
□ 其他
```

外加可选：

```text
备注
```

------

# **29. 数据保存策略**

正常试卷：

**默认不长期保存学生截图。**

保存结构化日志即可。

需要保存截图的情况：

```text
AI错判
need_review
人工主动标记
系统异常
```

这样避免大量保留学生作答图片。

------

# **30. 正常阅卷日志**

每一份至少保存：

```text
任务ID
卷序号
时间
模型
模型请求耗时
识别答案
各空得分
总分
need_review
判分依据
API是否成功
是否成功填写
是否成功提交
```

例如：

```json
{
  "index": 137,
  "provider": "deepseek",
  "latency_ms": 1384,
  "score": 6,
  "max_score": 7,
  "need_review": false,
  "submitted": true
}
```

------

# **31. 评分标准修改**

允许阅卷过程中暂停后修改评分规则。

修改后：

**只对后续试卷生效。**

已经批改过的试卷：

不重新分析。

记录评分规则版本号：

```text
Rule Version 1
Rule Version 2
```

这样日志可以知道每份试卷当时依据哪套标准。

------

# **32. 主界面建议**

保持极简。

大致：

```text
┌────────────────────────────────┐
│ AI阅卷助手                     │
├────────────────────────────────┤
│ 任务：暑假作业 第16题          │
│ DeepSeek：● 已连接             │
│ 状态：自动阅卷中               │
├────────────────────────────────┤
│ 当前识别                       │
│                                │
│ (1) 定                         │
│ (2) A                          │
│ (3) 钩码的数量                 │
│ (4) ……                         │
│                                │
├────────────────────────────────┤
│ 判分                           │
│ (1) 1/1 ✓                     │
│ (2) 1/1 ✓                     │
│ (3) 1/1 ✓                     │
│ (4) 0/1 ×                     │
│                                │
│ 总分：6 / 7                    │
│                                │
│ 扣分：第4空不满足……            │
├────────────────────────────────┤
│ 已处理：126                    │
│ 人工复核：4                    │
│ AI错判：2                      │
├────────────────────────────────┤
│ 提交倒计时：0.7s               │
│                                │
│ [暂停 F8] [标记错判]           │
└────────────────────────────────┘
```

不要做花哨 Dashboard。

核心是教师坐在旁边时：

一眼可以看明白 AI 刚才为什么给这个分。

------

# **33. 人工复核**

AI 请求复核后：

```text
自动暂停
```

界面显示：

```text
AI无法可靠判定

原因：
第（4）空存在明显涂改，无法确定最终保留的是
“增大”还是“减小”。

识别结果：
……

[0] [1] [2] ... [7]

[人工提交并继续]
```

允许用户直接给正确总分。

------

# **34. Platform Adapter**

即使 V1 只支持智学网，也定义：

```python
class GradingPlatform:
    capture_answer()
    enter_score(score)
    submit()
    wait_next()
```

具体：

```python
class ZhixuePlatform(GradingPlatform):
    ...
```

以后可以：

```text
OtherPlatform
```

不要让 GUI 直接调用 PyAutoGUI 点击坐标。

------

# **35. 安全保护**

以下必须属于核心功能，不是后续优化。

### **A. 分数验证**

非法分数禁止提交。

### **B. API JSON 验证**

解析失败禁止提交。

### **C. need_review**

禁止提交。

### **D. 网络失败**

禁止提交。

### **E. 用户暂停**

立即阻止下一次鼠标点击。

### **F. 提交前最终检查**

在执行：

```python
click(submit_button)
```

之前再次检查：

```text
automation_enabled
not paused
score_valid
AI_success
not need_review
```

### **G. 串行操作**

V1 不允许同时处理多份卷。

必须：

```text
一份 → 完成 → 下一份
```

------

# **36. 不要做的功能**

V1 明确禁止范围扩张：

- 不做学生管理。
- 不做成绩统计分析。
- 不做自动登录智学网。
- 不抓取班级名单。
- 不抓取学生身份数据。
- 不做浏览器插件。
- 不做手机端。
- 不做 Web App。
- 不做复杂 OCR pipeline。
- 不训练模型。
- 不做 Prompt 自动学习。
- 不做多电脑控制中心。
- 不做云同步。
- 不做自动修改以前已提交成绩。
- 不做已批试卷自动重跑。

------

# **37. Phase 0：Windows 技术验证**

**不要先写完整 UI。**

首先开发：

```text
prototype/
```

只验证以下事项。

### **Test 1**

Windows：

```text
mss
```

能正确截取指定屏幕区域。

### **Test 2**

截图可以发送 DeepSeek。

### **Test 3**

DeepSeek 能直接识别真实学生手写中文。

### **Test 4**

能够返回结构化评分 JSON。

### **Test 5**

PyAutoGUI 可以：

```text
点击分数框
Ctrl+A
输入整数
```

### **Test 6**

可以点击智学网提交按钮。

### **Test 7**

Windows：

```text
100%
125%
150%
```

DPI 环境下坐标行为确认。

优先推荐正式使用：

Windows 缩放 100%

但程序不能因为 DPI virtualization 出现严重坐标漂移。

### **Phase 0 验收**

用真实智学网页面：

人工控制开始 → AI判一份 → 自动填分 → 自动提交

至少连续成功：

```text
10份
```

再进入正式开发。

------

# **38. Phase 1：AI 阅卷核心**

完成：

- Task 数据结构。
- Question 数据结构。
- Rubric 数据结构。
- DeepSeek Provider。
- Prompt。
- JSON Schema。
- 输出验证。
- Dry Run。
- AI评分结果 UI。

### **验收**

提供至少 20 张真实学生答案截图。

程序能够批量测试并输出：

```text
识别结果
逐空评分
总分
理由
need_review
```

------

# **39. Phase 2：任务配置 UI**

实现：

- 新建任务。
- 编辑任务。
- 上传题图。
- 题目文字。
- 每空参考答案。
- 每空评分标准。
- 接受表达。
- 不接受表达。
- 单位规则。
- 满分。
- 步长。
- 全题补充规则。
- 自动保存。

### **验收**

完全不修改 JSON 文件即可配置一道真实题目。

------

# **40. Phase 3：Windows 自动化**

实现：

- 坐标标定。
- 截图。
- 自动填分。
- 自动提交。
- 快捷键。
- 安全暂停。
- 页面等待。
- API错误重试。

### **验收**

人工准备好评分标准后：

```text
启动
```

程序连续自动处理：

```text
20份
```

不得发生：

- 错位点击
- 空分提交
- API失败后提交
- need_review 仍提交

------

# **41. Phase 4：试改系统**

实现：

- 试改100份。
- 计数。
- AI错判标记。
- 错判原因。
- 截图留档。
- 漏报错判率。
- 自动模式参考阈值。

### **验收**

完成一次模拟 100 份阅卷流程。

输出报告：

```text
总数
AI正常
AI主动复核
AI错判
漏报错判率
```

------

# **42. Phase 5：自动模式**

实现：

- 自动阅卷。
- 连续处理限制。
- need_review 自动暂停。
- 网络错误暂停。
- API三次重试。
- 日志。
- 评分规则版本。

### **验收**

在测试环境连续运行至少：

```text
100份
```

不能因为单次错误造成后面整批错位。

------

# **43. Phase 6：Windows Portable**

使用：

```text
PyInstaller
```

输出：

```text
AIGrader-Windows-x64/
├── AIGrader.exe
└── ...
```

或者：

```text
AIGrader-Windows-x64.zip
```

要求：

- 无 Python 环境可运行。
- 不需要管理员权限。
- 不需要安装。
- 配置和数据库放用户数据目录。
- 更新程序不应自动删除历史任务。

------

# **44. GitHub Actions**

项目上传 GitHub。

建立：

```text
.github/workflows/windows-build.yml
```

使用：

```text
windows-latest
```

流程：

```text
checkout
↓
安装 Python
↓
pip install
↓
pytest
↓
PyInstaller
↓
打 ZIP
↓
上传 artifact
```

Release Tag 时自动生成：

```text
AIGrader-vX.Y.Z-Windows-x64.zip
```

因此：

Mac 上 Codex 可以正常开发。

最终 Windows exe：

由 GitHub Windows Runner 构建。

不允许尝试在 macOS 上交叉 PyInstaller Windows exe。

------

# **45. README**

必须写清：

## **使用条件**

推荐：

```text
Windows 10/11
浏览器窗口固定
Windows缩放100%
智学网页面布局固定
```

## **首次使用**

```text
1. 输入 DeepSeek API Key
2. 新建阅卷任务
3. 配置题目和评分标准
4. 标定答案区域
5. 标定分数输入框
6. 标定提交按钮
7. Dry Run测试
8. 开启试改模式
9. 确认准确率
10. 开启自动模式
```

------

# **46. 开发纪律**

请 Codex 严格遵守：

### **不要一次完成全部 Phase。**

每个 Phase：

```text
开发
→ 测试
→ README更新
→ Git Commit
→ 用户验收
```

再进入下一阶段。

### **每个阶段结束必须报告**

```text
1. 本阶段完成了什么
2. 修改了哪些文件
3. 如何运行
4. 如何测试
5. 已知问题
6. 下一阶段计划
```

不得只说：

已完成。

------

# **47. Codex 第一条执行指令**

可以把下面这一段接在任务书最后：

请先完整阅读本规格书，但现在不要直接开发完整 V1。

第一阶段只执行 **Phase 0：Windows 技术验证**。

首先检查当前仓库。如果仓库为空，请建立合理的 Python 项目骨架、`.gitignore`、README 和最基本依赖管理。

Phase 0 的目标不是界面美观，而是验证从“Windows 屏幕截图 → DeepSeek 视觉模型 → JSON 判分 → Windows 鼠标键盘操作 → 智学网提交”的整条链路真实可行。

DeepSeek API 层必须从第一天使用 Provider 抽象，不得将 API 调用直接耦合到 GUI 或自动化模块。智学网操作同样通过 Platform Adapter 隔离。

请优先写小型、可验证的 prototype，不要提前实现 Phase 1～6 的完整 UI。每完成一个技术验证，都留下能够单独运行的测试入口。

特别注意 Windows DPI、坐标系、PyAutoGUI failsafe、暂停机制和“任何异常绝不提交分数”的原则。

Phase 0 完成后，请停止开发并向我提交验收报告，不要自行继续 Phase 1。

------

## **一个我建议现在就加进项目的设计**

试改阶段不要只记录“AI错了几份”，最好自动区分：

```text
视觉识别错
        ↓
AI把学生字看错了

评分判断错
        ↓
字看对了，但评分标准执行错了

人工复核
        ↓
AI自己意识到不确定并暂停

漏报错判
        ↓
AI很自信地自动提交了错误分数
```

其中真正应该决定你敢不敢离开电脑的，是最后一项：

**漏报错判率。**

例如试改100份：

```text
AI主动暂停：7
人工检查后5份确实模糊
AI自动提交：93
其中错判：1

漏报错判率 = 1 / 93 ≈ 1.08%
```

这个指标比简单说“100份里面8份需要人工处理”有意义得多。

按照我们现在确定的需求，V1 已经不需要继续加功能了。先让 Codex 完成 Phase 0，尤其拿几张你真实的学生手写答案去验证 **DeepSeek 到底看得准不准**。这个验证结果会直接决定后面的开发值不值得继续。